
本代码可以说明代码块的执行顺序（普通代码块，构造代码块，静态代码块，ain函数代码块）：

1.static 代码块，所在类执行main函数或者实例化，都会首先运行该代码块；在实例化时，即使创建了多个实例对象，也仅执行一次
static{
/***/
}

2.static 代码块，代码顺序靠后的static代码块，仍会先于其他代码块执行
static{
/***/
}

3.main()方法，启动类，JVM调用时不实例化这个类的对象，而是通过类名直接调用main函数
publis static void main(Stringp[] args){
/***/
}

4.普通代码块，在所在类实例化对象的时候执行，否则不会执行
{
/***/
}

5.普通代码块，代码顺序靠后的普通代码块，仍会先于构造代码块执行
{
/***/
}

5.构造函数，在实例化对象时执行，排在静态代码块和普通代码块之后执行
public MyJavaCodeBlockTester(){
/***/
}
